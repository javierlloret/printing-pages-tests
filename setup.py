from setuptools import setup, find_packages

setup(
    name='printingpages',
    version='1.0.0',
    description='Testing a virtual printer',
    author='Javier Lloret',
    packages=find_packages()
)
